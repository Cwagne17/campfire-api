services:
  mongo:
    image: mongo:7
    container_name: campfire-api-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: campfire-api
    volumes:
      - campfire-mongo-data:/data/db

volumes:
  campfire-mongo-data:
